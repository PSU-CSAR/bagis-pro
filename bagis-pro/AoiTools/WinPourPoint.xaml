<controls:ProWindow x:Class="bagis_pro.AoiTools.WinPourPoint"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="Pour Points" Height="300" Width="300"
        WindowStartupLocation="CenterOwner"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid Loaded="MyGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="5">
            <ListBox x:Name="lstPourPoints" Grid.Row="1" ItemsSource="{Binding PourPoints}" Margin="5, 0, 0, 0" MinWidth="200" SelectedItem="{Binding SelectedPourPoint, Mode=TwoWay}" SelectionChanged="lstPourPoints_SelectionChanged">
            </ListBox>
        </DockPanel>
        <DockPanel Grid.Row="0" Grid.Column="1" >
            <Button x:Name="btnView" Content="View Basin" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="70" Command="{Binding CmdViewBasin}" Margin="15, 0, 0, 0" />
        </DockPanel>
        <DockPanel Grid.Row="1" Grid.Column="1" >
            <Button x:Name="btnNew" Content="New" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="70" Command="{Binding CmdNew}" Margin="15, 0, 0, 0" />
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="1" >
            <Button x:Name="btnSelect" Content="Select" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="70" Command="{Binding CmdSelect}" IsEnabled ="{Binding BtnSelectEnabled}" Margin="15, 0, 0, 0" />
        </DockPanel>
        <DockPanel Grid.Row="4" Grid.Column="1" >
            <Button x:Name="btnCancel" Content="Cancel" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="70" Command="{Binding CmdCancel}" Margin="15, 0, 0, 0" />
        </DockPanel>

    </Grid>
</controls:ProWindow>
