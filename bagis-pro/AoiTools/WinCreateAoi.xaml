<controls:ProWindow x:Class="bagis_pro.AoiTools.WinCreateAoi"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="Create Aoi" Height="300" Width="300"
        WindowStartupLocation="CenterOwner"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Center" Grid.ColumnSpan="2" VerticalAlignment="Center">
            <CheckBox x:Name="ckSnapPP" Content="Automatically snap pourpoint" IsChecked="{Binding SnapPPChecked}" Checked="CkSnapPP_Checked" Unchecked="CkSnapPP_Checked"/>
        </DockPanel>
        <DockPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Right" VerticalAlignment="Center">
            <TextBlock Text="Snap Distance:" Width="90" />
         </DockPanel>
        <DockPanel Grid.Row="1" Grid.Column="1"  HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBox x:Name="txtSnapDistance" Margin="5,0,0,0" Text="{Binding SnapDistance}" Height="20" Width="50" HorizontalAlignment="Left"/>
            <TextBlock Text="Meters" Width="90" Margin="5,0,0,0" VerticalAlignment="Center"/>
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Center" Grid.ColumnSpan="2" VerticalAlignment="Center">
            <CheckBox x:Name="ckAoiBuffer" Content="Buffer AOI to clip layers" IsChecked="{Binding AoiBufferChecked}" Checked="CkAoiBuffer_Checked" Unchecked="CkAoiBuffer_Checked"/>
        </DockPanel>
        <DockPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Right" VerticalAlignment="Center">
            <TextBlock Text="AOI Buffer Distance:" Width="120" />
        </DockPanel>
        <DockPanel Grid.Row="3" Grid.Column="1"  HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBox x:Name="txtBufferDistance" Margin="5,0,0,0" Text="{Binding AoiBufferDistance}" Height="20" Width="50" HorizontalAlignment="Left"/>
            <TextBlock Text="Meters" Width="90" Margin="5,0,0,0" VerticalAlignment="Center"/>
        </DockPanel>
        <DockPanel Grid.Row="4" Grid.Column="0" Margin="5,0,0,0" Grid.ColumnSpan="2">
            <TextBlock x:Name="lblBufferAoi" FontWeight="Bold" Foreground="Blue" MouseLeftButtonUp="LblBuffer_MouseLeftButtonUp" Width="110"><Run Text="(Why use a buffer?)"/></TextBlock>
        </DockPanel>
        <DockPanel Grid.Row="5" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Right" VerticalAlignment="Center">
            <TextBlock Text="DEM Elevation Unit:" Width="120" />
        </DockPanel>
        <DockPanel Grid.Row="5" Grid.Column="1" Margin="5,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" >
            <Label x:Name="lblElevUnits" Content="{Binding DemElevUnit}" Width="90"/>
        </DockPanel>
        <DockPanel Grid.Row="6" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Right" VerticalAlignment="Center">
            <TextBlock Text="Slope Unit:" Width="72" />
        </DockPanel>
        <DockPanel Grid.Row="7" Grid.Column="0" Margin="5,0,0,0" Grid.ColumnSpan="2">
            <Button x:Name="btnCreateAoi" Content="Generate AOI" HorizontalAlignment="Center" Width="130" FontWeight="Bold" VerticalAlignment="Center" Command="{Binding CmdGenerateAoi}" />
        </DockPanel>

    </Grid>
</controls:ProWindow>
