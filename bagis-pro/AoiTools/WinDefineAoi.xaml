<controls:ProWindow x:Class="bagis_pro.AoiTools.WinDefineAoi"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Height="300" Width="300"
        WindowStartupLocation="CenterOwner"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid Loaded="MyGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0" Margin="2,0,2,0">
            <TextBlock Name="tbBasinName" MaxWidth="100" VerticalAlignment="Center" HorizontalAlignment="Left" Text="Basin Name:" FontWeight="Bold" Margin="5, 0, 5, 0"/>
            <TextBlock x:Name="tbBasinNameValue" Text="{Binding Path=BasinName}" TextAlignment="Left" FontWeight="Bold" 
                                     VerticalAlignment="Center" Width="100"/>
        </DockPanel>
        <DockPanel Grid.Row="1" Grid.Column="0" Grid.RowSpan="5">
            <ListBox x:Name="lstAoi" Grid.Row="1" ItemsSource="{Binding AoiFolders}" Margin="5, 0, 0, 0">
            </ListBox>
        </DockPanel>
        <DockPanel Grid.Row="1" Grid.Column="1" >
            <Button x:Name="btnBoundary"  HorizontalAlignment="Center"  VerticalAlignment="Center" Width="20" Height="20" Command="{Binding CmdBoundary}" Margin="5, 0, 0, 0">
                <Image Source="pack://application:,,,/ArcGIS.Desktop.Resources;component/Images/EditingRectangleSquareTool16.png"/>
            </Button>
            <Button x:Name="btnNew" Content="New" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="70" Command="{Binding CmdNew}" Margin="10, 0, 0, 0" />
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="1" >
            <Button x:Name="btnSelect" Content="Select" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="70" Command="{Binding CmdSelectAoi}" Margin="15, 0, 0, 0" />
        </DockPanel>
        <DockPanel Grid.Row="3" Grid.Column="1" >
            <Button x:Name="btnDelete" Content="Delete" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="70" Command="{Binding CmdDeleteAoi}" Margin="15, 0, 0, 0" />
        </DockPanel>
        <DockPanel Grid.Row="4" Grid.Column="1" >
            <Button x:Name="btnClose" Content="Close" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="70" Command="{Binding CmdClose}" Margin="15, 0, 0, 0" />
        </DockPanel>

    </Grid>
</controls:ProWindow>
