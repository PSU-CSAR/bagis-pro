<controls:ProWindow x:Class="bagis_pro.Basin.WinBasinOptions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="Options" Height="250" Width="700"
        WindowStartupLocation="CenterOwner"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0" Margin="2,0,2,0">
            <TextBlock Name="tbReference" VerticalAlignment="Center" HorizontalAlignment="Left"  Text="Reference Layer" Margin="5, 0, 5, 0"/>
            <TextBox x:Name="txtRefLayer" Text="{Binding RefLayer}" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="300" IsReadOnly="True" />
        </DockPanel>
        <DockPanel Grid.Row="0" Grid.Column="1" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <Button x:Name="btnRefLayer" Content="Set" HorizontalAlignment="Left"  VerticalAlignment="Center" Width="50" Command="{Binding CmdRefLayer}" Margin="5, 0, 0, 0"/>
        </DockPanel>
        <DockPanel Grid.Row="0" Grid.Column="2" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <Button x:Name="btnAddRefLayer" Content="Add Ref. Layers to Map" FontWeight="Bold" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="150" Command="{Binding CmdRefLayer}" Margin="5, 0, 0, 0"/>
        </DockPanel>

        <DockPanel Grid.Row="1" Grid.Column="0" Margin="2,0,2,0">
            <TextBlock Name="tbDem" VerticalAlignment="Center" HorizontalAlignment="Left"  Text="DEM" Margin="5, 0, 5, 0"/>
            <TextBox x:Name="txtDem" Text="{Binding DemPath}" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="300" IsReadOnly="True"/>
        </DockPanel>
        <DockPanel Grid.Row="1" Grid.Column="1" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <Button x:Name="btnDem" Content="Set" HorizontalAlignment="Left"  VerticalAlignment="Center" Width="50" Command="{Binding CmdDem}" Margin="5, 0, 0, 0"/>
        </DockPanel>
        <DockPanel Grid.Row="1" Grid.Column="2" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0" VerticalAlignment="Center">
            <TextBlock HorizontalAlignment="Left"  Text="Elevation Unit" Margin="0, 0, 5, 0"/>
            <RadioButton GroupName="ElevUnits" Tag ="Meters" Margin="0, 0, 5, 0" IsChecked="{Binding Meters}">Meters</RadioButton>
            <RadioButton GroupName="ElevUnits" Tag ="Feet" Margin="0, 0, 0, 0" IsChecked="{Binding Feet}">Feet</RadioButton>
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="0" Margin="2,0,2,0">
            <TextBlock Name="tbGaugeStation" VerticalAlignment="Center" HorizontalAlignment="Left"  Text="Gauge Stations" Margin="5, 0, 5, 0"/>
            <TextBox x:Name="txtGaugeStation" Text="{Binding GaugeStation}" HorizontalAlignment="Right"  VerticalAlignment="Center" Width="300" IsReadOnly="True"/>
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="1" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <Button x:Name="btnGaugeStation" Content="Set" HorizontalAlignment="Left"  VerticalAlignment="Center" Width="50" Command="{Binding CmdGaugeStations}" Margin="5, 0, 0, 0"/>
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="2" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0" VerticalAlignment="Center">
            <TextBlock HorizontalAlignment="Left"  Text="Name Field" Margin="15,0,0,0"/>
            <TextBlock HorizontalAlignment="Left"  Text="Station Triplet Field" Margin="20,0,0,0"/>
        </DockPanel>
        <DockPanel Grid.Row="3" Grid.Column="2" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0" VerticalAlignment="Top">
            <ComboBox Name="cboName" SelectedItem="{Binding SelectedName}" HorizontalAlignment="Left" 
                ItemsSource="{Binding LstNames}"  Width="75" Height="20" Margin="10,0,0,0" />
            <ComboBox Name="cboStationTriplet" SelectedItem="{Binding SelectedTriplet}" HorizontalAlignment="Left" 
           ItemsSource="{Binding LstNames}"  Width="75" Height="20" Margin="15,0,0,0" />
        </DockPanel>
        <DockPanel Grid.Row="4" Grid.Column="2" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <Button x:Name="btnDefaultSettings" Content="Default Settings" FontWeight="Bold" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="120" Command="{Binding CmdDefault}"/>
        </DockPanel>
        <DockPanel Grid.Row="5" Grid.Column="2" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <Button x:Name="btnSaveSettings" Content="Save Settings" FontWeight="Bold" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="120" Command="{Binding CmdSave}"/>
        </DockPanel>
        <DockPanel Grid.Row="6" Grid.Column="2" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <Button x:Name="btnClose" Content="Close" FontWeight="Bold" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="120" Command="{Binding CmdClose}"/>
        </DockPanel>
        <DockPanel Grid.Row="6" Grid.Column="0" LastChildFill="true" KeyboardNavigation.TabNavigation="Local"  Margin="2,0,2,0">
            <TextBlock VerticalAlignment="Center"  Text="Note: These options apply only to Basin and AOI creation" Margin="15,0,0,0"/>
        </DockPanel>

    </Grid>
</controls:ProWindow>
